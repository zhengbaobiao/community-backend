<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.community.admin.mapper.AdminRoleMapper">

    <select id="selectRolesByUserId" resultType="com.community.admin.entity.AdminRole">
        SELECT r.*
        FROM admin_role r
        INNER JOIN admin_user_role ur ON r.id = ur.role_id
        WHERE ur.user_id = #{userId}
        AND r.status = 1
        AND r.deleted = 0
    </select>

</mapper>
